#include "sdt.h"
// This program is just an example one under VCS.
int main()
{
    float a, b;
    cout << "Input a:";
    cin >> a;
    cout << "Input b:";
    cin >> b;
    cout << " A + B = " << a + b << '\n';
    cout << " A - B = " << a - b << '\n';
}
